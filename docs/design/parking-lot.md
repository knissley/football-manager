# Parking lot

**Status: built.** Two tables. The first is ideas with a home that is not now: each
carries the milestone it would belong to, why it waits, and the trigger that would promote
it. The second is ideas ruled beyond the destination. The designer maintains both; nothing
here is forgotten and nothing here is scheduled.

| Idea | Would belong to | Why not now | What promotes it |
|---|---|---|---|
| Interference away from the ball: a flag on a receiver the throw was never going to | M5 | The crude resolver keeps no separation for anyone but the target after the throw (the simplification C2 and C15 admit) | The spatial engine carrying every matchup through the throw |
| Icing the kicker | M3 or later | Excluded from C16 by design; no evidence it changes outcomes enough to model | A sourced effect size, or a playtester asking for it |
| The five-yard penalty for a second excess injury timeout in a half (2025 rulebook, 4-5-4 Note 2) | M3 | Availability-only injuries make it unreachable | Injury severity (M3), and #122 |
| The ten-second runoff after a replay reversal or a nullified foul (4-7-1 Article 4) | never, unless replay exists | There is no replay system and no foul is nullified after the fact; pinned as not modelled | A replay and challenge system, which no milestone plans |
| An emergency kicker who is not a specialist | M3 | Rotation puts the backup specialist in; a position player kicking needs the playing-hurt and roster-emergency rules | #122 and the M3 injury work |
| Online leagues, iPad and Mac layouts, custom league import, 3D visualisation | after M9 | The roadmap's deferred list | A shipped beta |
| Alignment-dependent rating in the coverage contest: the slot rewarding `routeRunning`/`agility`/`acceleration` and the boundary `releaseVsPress`/`catchInTraffic`/`strength` | M5 | **Uncited, and CLAUDE.md rule 10 does not bend.** Nothing in `docs/reference/` or the `football-domain` skill says the slot and the boundary reward different traits, and no effect size exists for it. Item 2 of the [snap-share brief](briefs/snap-share-and-role-assignment.md), separated from its items 3 and 4 because the exploit it was bundled to counter turned out to be an ordering artifact that item 3 fixes on its own — so nothing accepted depends on it. It is also the only genuinely throwaway part of that brief: it edits `CrudeResolver`, which M5 deletes | A sourced line in `docs/reference/` — **or M5 arriving first**, where the spatial engine derives separation from geometry and alignment stops needing a rating key at all. If M5 wins the race this is never built as written, which is the right outcome for a rating model standing in for geometry one milestone away |

## Ruled out

Beyond the destination rather than not yet. A parked idea waits for a trigger; a ruled-out
one returns only if the vision or a milestone's exit is redrawn, and then as a fresh brief,
not a resumption. `docs/vision.md`'s *What this is not* holds the top-level rulings; this
table holds the ones made brief by brief, so a rejected idea is not re-pitched.

| Idea | Ruled out by | Why it is beyond the destination |
|---|---|---|
| _none yet_ | | |
