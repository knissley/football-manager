# Fixture: a run that sits on one line

Not a document. `scripts/lint-reference.sh --self-test` lints this file against
`../corpus.txt`, which is invented prose, not a rulebook.

This entry reproduces ten consecutive words of the fixture corpus, all of them on the
line below. A scan that looks at each line on its own finds it, which is why it is the
easy case and why it is not the case that matters.

- **7-3-1** — Our note says a runner may hand the baton to a team mate; nothing else.
